package LearningJavaAndInstruments.Git;

public class GitDescription {
    // git --version      -show in terminal version of GIT
    // git config --global( or local) user.name (declare our name) RomanIvanov   and change the same way      // declare name of user
    // git config --global user.email Roamn22091995@yandex.ru           // declare email of user
    // git config (user.name / user.email)   // show configuration

    // git --help   // show all possibilities and methods of git
    // ls   - choose our file in idea and use it to show all file in directory
    // cd - change directory - choose next File and use ls to needed file

    // git init  // start init the project after this was created hidden folder is .git
    // cd .. - return to level project or write directory
    // cd ~ / return to package home

    // git status // show commits and status git
    // git add () // show git to changed to be commited

    // git rm --cached <file> - delete file
    // git add . // add all files
    //git commit -m // like mesage// "put here comment" - add file to local storage

    // create file// .girignore // and write there names of files // to ignore some files or types
     // / (file name) in .gitignore to ignore the package


    // git branch // show us where we place on the branch and other branches
    // git branch (nameOfBranch) // create new branch
    // git branch -D (nameOfBrach) // delete the branch

    // git checkout (nameOfBranch) // switch to other branch
    // git checkout -b (nameOfBranch) // create branch and switch there


    // git merge (nameOfFile) // combine two branches

    // connect git and gitHub
    // if we didn't do initiation of Git , do that show on the GiHub site
    // and after copy link which connect Git  and GitHub
    //git remote add origin https://github.com/RomanIvanov22091995/learningGIT.git
    // git push -u origin main  - its send files to GitHub




}
